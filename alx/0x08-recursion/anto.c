#include <stdio.h>
#include <math.h>

int main()
{
    int n = 14;
    float p = sqrt(n); // Calculate the square root of n and store it in p.
    printf("%f\n", p); // Print the result.

    return 0; // Indicates successful program execution.
}
